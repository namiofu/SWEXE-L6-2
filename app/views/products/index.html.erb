<% @products.each do |my| %>
   <p> 
       <%= my.name %>
       <%= my.price %>円
       <%= link_to '削除', "/products/#{my.id}", method: :delete %>
       <%= link_to 'カート追加', new_cartitem_path(product_id: my.id) %>
    </p>
<% end %>
<p><%= link_to 'カート一覧', cart_path(current_cart.id) %></p>
<p><%= link_to '新規追加', new_product_path %></p>